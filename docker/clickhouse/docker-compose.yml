services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.8
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      CLICKHOUSE_DB: liteness
      # Without a password the image disables HTTP access for user `default`.
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: liteness
